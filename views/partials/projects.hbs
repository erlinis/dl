 <div class='card'>
  <div class='card-containers'>
    <div class='card-header'>
      <h3>
        {{#if framework}}
          {{framework}}
        {{else}}
          {{platform}}
        {{/if}} Projects</h3>
    </div>
    <div class='card-body'>
        {{> projectInfoTitles framework='Sinatra' platform='Ruby'}}
        {{> projectsInfo projects=projects}}
    </div>
  </div>
</div>
